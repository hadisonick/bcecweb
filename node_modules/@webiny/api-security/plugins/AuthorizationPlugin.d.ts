import { Plugin } from "@webiny/plugins";
import { SecurityContext, SecurityPermission } from "../types";
export declare abstract class AuthorizationPlugin<TContext = SecurityContext> extends Plugin {
    static readonly type = "security-authorization";
    abstract getPermissions(context: TContext): Promise<SecurityPermission[]>;
}
