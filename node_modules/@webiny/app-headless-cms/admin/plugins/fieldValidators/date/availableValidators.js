export var getAvailableValidators = function getAvailableValidators(type) {
  var validators = ["required"];

  if (!type) {
    return validators;
  } else if (type === "time") {
    return validators.concat(["timeGte", "timeLte"]);
  }

  return validators.concat(["dateGte", "dateLte"]);
};
//# sourceMappingURL=availableValidators.js.map