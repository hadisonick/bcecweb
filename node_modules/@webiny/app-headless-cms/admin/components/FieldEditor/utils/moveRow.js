import _toConsumableArray from "@babel/runtime/helpers/toConsumableArray";
import dot from "dot-prop-immutable";
export default (function (_ref) {
  var data = _ref.data,
      source = _ref.source,
      destination = _ref.destination;
  return dot.set(data, "layout", function (layout) {
    return source < destination ? [].concat(_toConsumableArray(layout.slice(0, source)), _toConsumableArray(layout.slice(source + 1, destination)), [layout[source]], _toConsumableArray(layout.slice(destination))) : [].concat(_toConsumableArray(layout.slice(0, destination)), [layout[source]], _toConsumableArray(layout.slice(destination, source)), _toConsumableArray(layout.slice(source + 1)));
  });
});
//# sourceMappingURL=moveRow.js.map