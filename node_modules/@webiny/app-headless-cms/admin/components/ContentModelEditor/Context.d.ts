import React from "react";
import { ApolloClient } from "apollo-client";
import { CmsEditorContentModel } from "../../../types";
export interface Context {
    apolloClient: ApolloClient<any>;
    data: CmsEditorContentModel;
    isPristine: boolean;
    getContentModel: (modelId: string) => Promise<any>;
    saveContentModel: (data?: Record<string, any>) => Promise<any>;
    setData: (setter: Function, saveContentModel?: boolean) => Promise<any>;
}
export declare const contentModelEditorContext: React.Context<Context>;
export declare function contentModelEditorReducer(prev: any, { data, type }: {
    data: any;
    type: any;
}): any;
interface Props {
    apolloClient: ApolloClient<any>;
    modelId: string;
    children: React.ReactElement;
}
export declare function ContentModelEditorProvider({ children, apolloClient, modelId }: Props): JSX.Element;
export {};
