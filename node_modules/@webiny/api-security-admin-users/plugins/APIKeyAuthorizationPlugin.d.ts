import { AdminUsersContext } from "../types";
import { AuthorizationPlugin } from "@webiny/api-security/plugins/AuthorizationPlugin";
export interface Config {
    identityType?: string;
}
export declare class APIKeyAuthorizationPlugin extends AuthorizationPlugin<AdminUsersContext> {
    private _config;
    constructor(config?: Config);
    getPermissions({ security }: AdminUsersContext): Promise<any[]>;
}
