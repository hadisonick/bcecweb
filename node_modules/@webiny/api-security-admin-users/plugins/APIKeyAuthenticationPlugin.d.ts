import { Context as HandlerContext } from "@webiny/handler/types";
import { HttpContext } from "@webiny/handler-http/types";
import { SecurityIdentity } from "@webiny/api-security";
import { AuthenticationPlugin } from "@webiny/api-security/plugins/AuthenticationPlugin";
import { TenancyContext } from "@webiny/api-tenancy/types";
declare type Context = HandlerContext<HttpContext, TenancyContext>;
export interface Config {
    identityType?: string;
}
export declare class APIKeyAuthenticationPlugin extends AuthenticationPlugin<Context> {
    private _config;
    constructor(config?: Config);
    authenticate(context: Context): Promise<SecurityIdentity>;
}
export {};
