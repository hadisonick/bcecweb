import { Plugin } from "@webiny/plugins";
import { AdminUsersContext } from "../types";
export declare abstract class Base {
    protected context: AdminUsersContext;
    constructor(context: AdminUsersContext);
    abstract get plugins(): Plugin[];
    protected executeCallback<TCallbackFunction extends (params: any) => void | Promise<void>>(callback: string, params: Parameters<TCallbackFunction>[0]): Promise<void>;
}
