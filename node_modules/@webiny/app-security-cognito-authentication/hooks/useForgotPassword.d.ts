export interface ForgotPassword {
    shouldRender: boolean;
    requestCode(params: {
        username: string;
    }): Promise<void>;
    setPassword(params: {
        username: string;
    }): Promise<void>;
    codeSent: boolean;
    error: Error;
    loading: boolean;
}
export declare function useForgotPassword(): ForgotPassword;
