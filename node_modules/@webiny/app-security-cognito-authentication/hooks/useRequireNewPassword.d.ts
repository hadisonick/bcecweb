export interface RequireNewPassword {
    shouldRender: boolean;
    confirm(params: {
        password: string;
        requiredAttributes: {
            [key: string]: string;
        };
    }): Promise<void>;
    requiredAttributes: string[];
}
export declare function useRequireNewPassword(): RequireNewPassword;
