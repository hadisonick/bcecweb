export interface SetNewPassword {
    shouldRender: boolean;
    setPassword(params: {
        code: string;
        password: string;
    }): Promise<void>;
    error: Error;
    loading: boolean;
}
export declare function useSetNewPassword(): SetNewPassword;
