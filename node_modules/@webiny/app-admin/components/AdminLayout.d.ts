import React from "react";
interface AdminLayoutProps {
    title?: string;
    children: React.ReactNode;
}
export declare const AdminLayout: ({ title, children }: AdminLayoutProps) => JSX.Element;
export {};
