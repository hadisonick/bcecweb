import { Plugin } from "@webiny/plugins";
import { ElasticsearchQueryModifierPluginParams } from "../../types";
interface Callable<T extends ElasticsearchQueryModifierPluginParams = ElasticsearchQueryModifierPluginParams> {
    (params: T): string;
}
export declare class ElasticsearchQueryModifierPlugin extends Plugin {
    static readonly type = "elasticsearch.queryBuilder.modifier";
    private readonly _callable?;
    constructor(callable?: Callable);
    apply<P extends ElasticsearchQueryModifierPluginParams = ElasticsearchQueryModifierPluginParams>(params: P): void;
}
export {};
