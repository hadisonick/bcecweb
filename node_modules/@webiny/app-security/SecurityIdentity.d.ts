import { SecurityPermission } from "./types";
export declare type SecurityIdentityData = {
    login: string;
    permissions?: SecurityPermission[];
    logout(): void;
    [key: string]: any;
    getPermission?(permission: string): SecurityPermission;
};
export declare class SecurityIdentity {
    login: string;
    permissions: SecurityPermission[];
    [key: string]: any;
    constructor(data: SecurityIdentityData);
    /**
     * Create a new instance of SecurityIdentity from the existing instance.
     */
    static from(identity: SecurityIdentity, data: Partial<SecurityIdentityData>): SecurityIdentity;
    setPermissions(permissions: SecurityPermission[]): void;
    getPermission<T extends SecurityPermission = SecurityPermission>(name: string): T;
    logout(): void;
}
