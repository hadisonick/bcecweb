import React from "react";
export declare const TenancyContext: React.Context<any>;
export interface Tenant {
    id: string;
    name: string;
}
export declare type TenancyContextValue<TTenant = Tenant> = {
    tenant: TTenant | null;
    tenants: TTenant[];
    setTenant(tenant: TTenant): void;
    onChange(cb: (tenant: TTenant) => void): any;
};
export declare const TenancyProvider: (props: any) => JSX.Element;
