import { Tag } from "../queue/add/types";
declare type Args = {
    tag: Tag;
    url?: string;
    dbNamespace?: string;
};
declare const _default: ({ tag, url, dbNamespace }: Args) => [string, string];
export default _default;
