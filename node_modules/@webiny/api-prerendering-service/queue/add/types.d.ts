import { HandlerPlugin as DefaultHandlerPlugin, Context } from "@webiny/handler/types";
import { ArgsContext } from "@webiny/handler-args/types";
import { DbContext } from "@webiny/handler-db/types";
import { Args as FlushArgs } from "./../../flush/types";
import { Args as RenderArgs } from "./../../render/types";
import { Plugin } from "@webiny/plugins/types";
export declare type Tag = {
    key: string;
    value?: string;
};
export declare type QueueArgs = {
    tag?: Tag;
};
export declare type Args = {
    flush?: FlushArgs & QueueArgs;
    render?: RenderArgs & QueueArgs;
};
export declare type HandlerArgs = Args | Args[];
export declare type HandlerContext = Context<DbContext, ArgsContext<HandlerArgs>>;
export declare type HandlerPlugin = DefaultHandlerPlugin<HandlerContext>;
export declare type HookCallbackFunction = (args: {
    log: any;
    context: HandlerContext;
    args: Args;
}) => void | Promise<void>;
export declare type QueueAddHookPlugin = Plugin<{
    type: "ps-queue-add-hook";
    beforeAdd?: HookCallbackFunction;
    afterAdd?: HookCallbackFunction;
}>;
