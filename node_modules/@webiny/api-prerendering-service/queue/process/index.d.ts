import { HandlerPlugin } from "./types";
declare type Configuration = {
    handlers: {
        render: string;
        flush: string;
    };
};
declare const _default: (configuration: Configuration) => HandlerPlugin;
export default _default;
