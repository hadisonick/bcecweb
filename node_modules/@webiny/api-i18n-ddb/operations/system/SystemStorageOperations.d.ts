import { I18NContext, I18NSystem, I18NSystemStorageOperations } from "@webiny/api-i18n/types";
interface ConstructorParams {
    context: I18NContext;
}
export declare class SystemStorageOperations implements I18NSystemStorageOperations {
    private readonly _context;
    private readonly _entity;
    private get partitionKey();
    constructor({ context }: ConstructorParams);
    get(): Promise<I18NSystem>;
    create({ system }: {
        system: any;
    }): Promise<I18NSystem>;
    update({ system }: {
        system: any;
    }): Promise<I18NSystem>;
}
export {};
