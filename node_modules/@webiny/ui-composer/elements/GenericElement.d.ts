import React from "react";
import { UIElement } from "../UIElement";
interface Renderer<TRenderProps> {
    (props: TRenderProps): React.ReactNode;
}
export declare class GenericElement<TRenderProps = any> extends UIElement {
    private _render;
    constructor(id: any, render?: Renderer<TRenderProps>);
    render(props: TRenderProps): {};
}
export {};
