/// <reference types="react" />
export interface UIRenderParams<TElement, TProps = any> {
    element: TElement;
    props: TProps;
    next: RenderNext;
    children: RenderChildren;
}
export interface RenderNext {
    (): React.ReactNode;
}
export interface RenderChildren {
    (): React.ReactNode;
}
export declare abstract class UIRenderer<TElement, TProps = any> {
    canRender(element: TElement, props: TProps): boolean;
    abstract render(params: UIRenderParams<TElement, TProps>): React.ReactNode;
}
