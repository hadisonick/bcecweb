import { UIView } from "@webiny/app-admin/ui/UIView";
import { Form } from "@webiny/form";
import { UseUserForm } from "./hooks/useUserForm";
export declare class UsersFormView extends UIView {
    constructor();
    getUserFormHook(): UseUserForm;
    submit(data: FormData, form?: Form): void;
    onSubmit(cb: (data: any, form: Form) => void): void;
    private addElements;
}
