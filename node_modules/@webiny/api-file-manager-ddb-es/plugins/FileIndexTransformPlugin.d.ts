import { Plugin } from "@webiny/plugins";
import { File } from "@webiny/api-file-manager/types";
import { FileIndexTransformFromConfig, FileIndexTransformPluginConfig, FileIndexTransformToConfig } from "../types";
export declare class FileIndexTransformPlugin extends Plugin {
    static readonly type = "fm.ddbEs.file.index";
    private readonly _params;
    constructor(params: FileIndexTransformPluginConfig);
    /**
     * Be aware that you must return the whole file object.
     */
    toIndex(params: FileIndexTransformToConfig): Promise<File>;
    /**
     * Be aware that you must return the whole file object.
     * This method MUST reverse what ever toIndex method changed on the file object.
     */
    fromIndex(params: FileIndexTransformFromConfig): Promise<File>;
}
