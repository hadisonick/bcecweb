"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.transformFromIndex = exports.transformToIndex = void 0;

var _defineProperty2 = _interopRequireDefault(require("@babel/runtime/helpers/defineProperty"));



const transformToIndex = async ({
  plugins,
  file
}) => {
  const originalFile = _objectSpread({}, file);

  let indexFile = _objectSpread({}, file);

  for (const plugin of plugins) {
    indexFile = await plugin.toIndex({
      original: originalFile,
      file: indexFile
    });
  }

  return indexFile;
};

exports.transformToIndex = transformToIndex;

const transformFromIndex = async ({
  plugins,
  file
}) => {
  let original = _objectSpread({}, file);

  for (const plugin of plugins) {
    original = await plugin.fromIndex({
      file: original
    });
  }

  return original;
};

exports.transformFromIndex = transformFromIndex;
//# sourceMappingURL=transformers.js.map