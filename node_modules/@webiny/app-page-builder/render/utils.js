import orderBy from "lodash/orderBy";
import { plugins } from "@webiny/plugins";
export var applyPerDeviceStyleWithFallback = function applyPerDeviceStyleWithFallback(applyStyle) {
  // Get display modes
  var displayModeConfigs = plugins.byType("pb-render-responsive-mode").map(function (pl) {
    return pl.config;
  }); // Set per-device property value

  orderBy(displayModeConfigs, "minWidth", "desc").forEach(function (_ref, index, arr) {
    var displayMode = _ref.displayMode;
    var fallbackMode = index > 0 ? arr[index - 1].displayMode : "desktop"; // Apply style

    applyStyle({
      fallbackMode: fallbackMode,
      displayMode: displayMode
    });
  });
};
//# sourceMappingURL=utils.js.map