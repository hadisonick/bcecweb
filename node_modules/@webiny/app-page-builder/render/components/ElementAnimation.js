import { useEffect } from "react";
import aos from "aos";
import "aos/dist/aos.css";
import once from "lodash/once";
import throttle from "lodash/throttle";
var init = once(function () {
  return setTimeout(aos.init, 500);
});
var refresh = throttle(aos.refreshHard, 250);

var ElementAnimation = function ElementAnimation(_ref) {
  var children = _ref.children;
  useEffect(function () {
    init();
    refresh();
  }, []);

  if (typeof children === "function") {
    return children({
      init: init,
      refresh: refresh
    });
  }

  return children;
};

export default ElementAnimation;
//# sourceMappingURL=ElementAnimation.js.map