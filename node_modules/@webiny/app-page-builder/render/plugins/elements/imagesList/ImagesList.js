import * as React from "react";
import warning from "warning";
import { plugins } from "@webiny/plugins";

var ImagesList = function ImagesList(_ref) {
  var data = _ref.data,
      theme = _ref.theme;
  var component = data.component;
  var listComponentPlugins = plugins.byType("pb-page-element-images-list-component");
  var imagesList = listComponentPlugins.find(function (cmp) {
    return cmp.componentName === component;
  });

  if (!imagesList) {
    warning(false, "Images list component \"".concat(component, "\" is missing!"));
    return null;
  }

  var ListComponent = imagesList.component;

  if (!ListComponent) {
    warning(false, "React component is not defined for \"".concat(component, "\"!"));
    return null;
  }

  return /*#__PURE__*/React.createElement(ListComponent, {
    data: data.images || [],
    theme: theme
  });
};

export default /*#__PURE__*/React.memo(ImagesList);
//# sourceMappingURL=ImagesList.js.map