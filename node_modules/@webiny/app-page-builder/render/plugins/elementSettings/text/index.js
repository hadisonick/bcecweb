import _get from "lodash/get";
import kebabCase from "lodash/kebabCase";
import { applyPerDeviceStyleWithFallback } from "../../../utils";
export default {
  name: "pb-render-page-element-style-text",
  type: "pb-render-page-element-style",
  renderStyle: function renderStyle(_ref) {
    var element = _ref.element,
        style = _ref.style;

    var textSettings = _get(element, "data.text", {}); // Set per-device property value


    applyPerDeviceStyleWithFallback(function (_ref2) {
      var displayMode = _ref2.displayMode,
          fallbackMode = _ref2.fallbackMode;
      // Set text color
      style["--".concat(kebabCase(displayMode), "-color")] = _get(textSettings, "".concat(displayMode, ".color"), _get(style, "--".concat(kebabCase(fallbackMode), "-color"), "inherit")); // Set text alignment

      style["--".concat(kebabCase(displayMode), "-text-align")] = _get(textSettings, "".concat(displayMode, ".alignment"), _get(style, "--".concat(kebabCase(fallbackMode), "-text-align"), "inherit"));
    });
    return style;
  }
};
//# sourceMappingURL=index.js.map