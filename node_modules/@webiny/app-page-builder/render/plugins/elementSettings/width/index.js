import get from "lodash/get";
import kebabCase from "lodash/kebabCase";
import { applyPerDeviceStyleWithFallback } from "../../../utils";
export default {
  name: "pb-render-page-element-style-width",
  type: "pb-render-page-element-style",
  renderStyle: function renderStyle(_ref) {
    var element = _ref.element,
        style = _ref.style;

    var _get = get(element, "data.settings", {}),
        width = _get.width;

    applyPerDeviceStyleWithFallback(function (_ref2) {
      var displayMode = _ref2.displayMode,
          fallbackMode = _ref2.fallbackMode;
      var fallbackValue = get(style, "--".concat(kebabCase(fallbackMode), "-width"), "100%");
      style["--".concat(kebabCase(displayMode), "-width")] = get(width, "".concat(displayMode, ".value"), fallbackValue);
    });
    return style;
  }
};
//# sourceMappingURL=index.js.map