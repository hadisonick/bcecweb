import _slicedToArray from "@babel/runtime/helpers/slicedToArray";
import { useEffect, useState } from "react";
import { pingSite } from "./utils";
export var useSiteStatus = function useSiteStatus(url) {
  var _useState = useState(false),
      _useState2 = _slicedToArray(_useState, 2),
      active = _useState2[0],
      setActive = _useState2[1];

  useEffect(function () {
    if (url) {
      pingSite({
        url: url,
        cb: setActive,
        ignoreCache: false
      });
    }
  }, [url]);
  return [active, function () {
    pingSite({
      url: url,
      cb: setActive,
      ignoreCache: true
    });
  }];
};
//# sourceMappingURL=useSiteStatus.js.map