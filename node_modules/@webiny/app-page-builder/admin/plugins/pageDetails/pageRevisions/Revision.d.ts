/// <reference types="react" />
import { PbPageData, PbPageRevision } from "../../../../types";
declare type RevisionProps = {
    revision: PbPageRevision;
    page: PbPageData;
};
declare const Revision: ({ revision, page }: RevisionProps) => JSX.Element;
export default Revision;
