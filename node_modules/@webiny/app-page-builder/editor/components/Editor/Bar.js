import _createForOfIteratorHelper from "@babel/runtime/helpers/createForOfIteratorHelper";
import React from "react";
import DefaultEditorBar from "./DefaultEditorBar";
import { activeElementAtom, pluginsAtom } from "../../recoil/modules";
import { plugins } from "@webiny/plugins";
import { useRecoilValue } from "recoil";

var Bar = function Bar() {
  var activeElementId = useRecoilValue(activeElementAtom);
  var editorPlugins = useRecoilValue(pluginsAtom);
  var pluginsByType = plugins.byType("pb-editor-bar");
  var pluginBar = null;

  var _iterator = _createForOfIteratorHelper(pluginsByType),
      _step;

  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var plugin = _step.value;

      if (plugin.shouldRender({
        plugins: editorPlugins,
        activeElement: activeElementId
      })) {
        pluginBar = plugin.render();
        break;
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }

  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(DefaultEditorBar, null), pluginBar);
};

export default /*#__PURE__*/React.memo(Bar);
//# sourceMappingURL=Bar.js.map