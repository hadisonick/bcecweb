import React, { SyntheticEvent } from "react";
declare type ResizerProps = {
    axis: "x" | "y";
    onResizeStart?: () => void;
    onResizeStop?: () => void;
    onResize(position: number): void;
    children(params: {
        isResizing: boolean;
        onMouseDown(e: SyntheticEvent): void;
    }): React.ReactElement;
};
declare const Resizer: ({ axis, onResizeStart, onResizeStop, onResize, children }: ResizerProps) => React.ReactElement<any, string | ((props: any) => React.ReactElement<any, string | any | (new (props: any) => React.Component<any, any, any>)>) | (new (props: any) => React.Component<any, any, any>)>;
export default Resizer;
