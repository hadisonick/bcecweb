import React, { ReactElement } from "react";
import { ConnectDragSource, DragSourceMonitor, DragObjectWithType } from "react-dnd";
export declare type DraggableChildrenFunction = (params: {
    isDragging: boolean;
    drag: ConnectDragSource;
}) => ReactElement;
export declare type DraggableProps = {
    children: DraggableChildrenFunction;
    beginDrag(props: DraggableProps, monitor: DragSourceMonitor): any;
    endDrag(item: any, monitor: DragSourceMonitor): void;
    target: string[];
    enabled: boolean;
};
export declare type DraggableItem = DragObjectWithType & {
    target: string[];
};
declare const Draggable: React.MemoExoticComponent<(props: DraggableProps) => JSX.Element>;
export default Draggable;
