import { EventAction, EventActionOptionsType } from "./EventAction";
export declare abstract class BaseEventAction<T extends object = any> implements EventAction<T> {
    private readonly _args;
    private readonly _options;
    constructor(args?: T, options?: EventActionOptionsType);
    abstract getName(): string;
    getArgs(): T;
    getOptions(): EventActionOptionsType;
}
