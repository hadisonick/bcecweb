import _defineProperty from "@babel/runtime/helpers/defineProperty";
import _objectSpread from "@babel/runtime/helpers/objectSpread2";
import { plugins } from "@webiny/plugins";
export var activatePluginByNameMutation = function activatePluginByNameMutation(state, name) {
  var _ref = plugins.byName(name) || {},
      type = _ref.type;

  if (!type) {
    return state;
  }

  var allPluginsByType = state[type] || [];
  var exists = allPluginsByType.some(function (pl) {
    return pl.name === name;
  });

  if (exists) {
    return state;
  }

  return _objectSpread(_objectSpread({}, state), {}, _defineProperty({}, type, allPluginsByType.concat([{
    name: name
  }])));
};
//# sourceMappingURL=activatePluginMutation.js.map