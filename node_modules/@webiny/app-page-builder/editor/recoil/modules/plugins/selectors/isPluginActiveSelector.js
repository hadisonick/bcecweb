import { plugins } from "@webiny/plugins";
import { selectorFamily } from "recoil";
import { pluginsAtom } from "../pluginsAtom";
export var isPluginActiveSelector = selectorFamily({
  key: "isPluginActiveSelector",
  get: function get(name) {
    return function (_ref) {
      var get = _ref.get;
      var target = plugins.byName(name);
      var state = get(pluginsAtom);

      if (!target) {
        return false;
      }

      return isPluginActive(state, target);
    };
  }
});
export var isPluginActive = function isPluginActive(state, target) {
  var type = target.type;

  if (!state[type]) {
    return false;
  }

  var list = state[type];
  return list.some(function (_ref2) {
    var name = _ref2.name;
    return name === target.name;
  });
};
//# sourceMappingURL=isPluginActiveSelector.js.map