import { selectorFamily } from "recoil";
import { pluginsAtom } from "../pluginsAtom";
export var activePluginsByTypeNamesSelector = selectorFamily({
  key: "activePluginsByTypeNamesSelector",
  get: function get(type) {
    return function (_ref) {
      var get = _ref.get;
      var activePlugins = get(pluginsAtom);
      var pluginsByType = activePlugins[type];

      if (!pluginsByType || pluginsByType.length === 0) {
        return [];
      }

      return pluginsByType.map(function (p) {
        return p.name;
      });
    };
  }
});
//# sourceMappingURL=activePluginsByTypeNamesSelector.js.map