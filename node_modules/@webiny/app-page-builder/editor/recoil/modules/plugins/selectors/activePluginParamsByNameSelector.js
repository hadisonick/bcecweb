import { plugins } from "@webiny/plugins";
import { selectorFamily } from "recoil";
import { pluginsAtom } from "../..";
export var activePluginParamsByNameSelector = selectorFamily({
  key: "activePluginParamsByNameSelector",
  get: function get(name) {
    return function (_ref) {
      var get = _ref.get;

      var _plugins$byName = plugins.byName(name),
          type = _plugins$byName.type;

      if (!type) {
        return null;
      }

      var pluginsAtomValue = get(pluginsAtom);
      var pluginsByType = pluginsAtomValue[type] || [];
      var activePlugin = pluginsByType.find(function (pl) {
        return pl.name === name;
      });

      if (!activePlugin) {
        return null;
      }

      return activePlugin.params || null;
    };
  }
});
//# sourceMappingURL=activePluginParamsByNameSelector.js.map