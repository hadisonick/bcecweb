import { atom } from "recoil";
import { DisplayMode } from "../../../../types";
export var uiAtom = atom({
  key: "uiAtom",
  default: {
    isDragging: false,
    isResizing: false,
    isSaving: false,
    displayMode: DisplayMode.DESKTOP,
    pagePreviewDimension: {
      width: 100,
      height: 100
    }
  }
});
//# sourceMappingURL=uiAtom.js.map