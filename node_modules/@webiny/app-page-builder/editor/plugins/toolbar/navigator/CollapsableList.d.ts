import React, { ReactElement } from "react";
import { HighlightItem } from "./StyledComponents";
declare type CollapsableListProps = {
    children: ReactElement | ReactElement[];
    header: ReactElement;
    level: number;
    disableAction: boolean;
    active: boolean;
    style?: React.CSSProperties;
    headerStyle?: React.CSSProperties;
    inActivePath: boolean;
    highlightItem: HighlightItem;
};
declare const CollapsableList: ({ children, header, level, disableAction, active, style, headerStyle, inActivePath, highlightItem }: CollapsableListProps) => JSX.Element;
export default CollapsableList;
