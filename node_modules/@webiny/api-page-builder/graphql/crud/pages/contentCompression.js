"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.compressContent = exports.extractContent = void 0;

var _jsonpack = _interopRequireDefault(require("jsonpack"));

const extractContent = contentProp => {
  if (!contentProp || !contentProp.compression) {
    return null;
  }

  try {
    return _jsonpack.default.unpack(contentProp.content);
  } catch {
    return null;
  }
};

exports.extractContent = extractContent;

const compressContent = (content = null) => {
  let compressed = null;

  if (content) {
    try {
      compressed = _jsonpack.default.pack(content);
    } catch {}
  }

  return {
    compression: "jsonpack",
    content: compressed
  };
};

exports.compressContent = compressContent;
//# sourceMappingURL=contentCompression.js.map