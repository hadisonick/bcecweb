import { Options as CognitOptions } from "@webiny/app-security-cognito-authentication";
import { PluginCollection } from "@webiny/plugins/types";
import { Props } from "./Authentication";
export interface PasswordPolicy {
    minimumLength?: number;
    requireLowercase?: boolean;
    requireNumbers?: boolean;
    requireSymbols?: boolean;
    requireUppercase?: boolean;
}
export interface Options extends CognitOptions {
    passwordPolicy?: PasswordPolicy;
    getIdentityData: Props["getIdentityData"];
}
declare const _default: (options: Options) => PluginCollection;
export default _default;
