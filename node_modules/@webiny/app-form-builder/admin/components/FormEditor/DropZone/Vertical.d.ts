/// <reference types="react" />
import { DragObjectWithType } from "react-dnd";
declare type VerticalProps = {
    onDrop(item: DragObjectWithType): void;
    last?: boolean;
    isVisible?: any;
};
declare const Vertical: ({ last, onDrop, isVisible }: VerticalProps) => JSX.Element;
export default Vertical;
