import * as React from "react";
import { DragObjectWithType } from "react-dnd";
declare type Props = {
    type?: string;
    onDrop(item: DragObjectWithType): void;
    children: React.ReactNode;
    active?: boolean;
    highlight?: boolean;
};
export default function Center({ onDrop, children }: Props): JSX.Element;
export {};
