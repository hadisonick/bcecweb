/// <reference types="react" />
import { DragObjectWithType } from "react-dnd";
declare type HorizontalProps = {
    onDrop(item: DragObjectWithType): void;
    last?: boolean;
    isVisible?: any;
};
declare const Horizontal: ({ last, onDrop, isVisible }: HorizontalProps) => JSX.Element;
export default Horizontal;
