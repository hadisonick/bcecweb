import { FbFormModel } from "../../../../../types";
declare const _default: ({ data, source, destination }: {
    source: number;
    destination: number;
    data: FbFormModel;
}) => void;
export default _default;
