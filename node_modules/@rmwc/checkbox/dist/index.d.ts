import * as RMWC from '@rmwc/types';
import * as React from 'react';
import { MDCCheckboxFoundation } from '@material/checkbox';
import { ToggleableFoundationComponent, ToggleableFoundationProps } from '@rmwc/toggleable';
/** A Checkbox component. */
export interface CheckboxProps extends RMWC.WithRippleProps, ToggleableFoundationProps {
    /** Make the control indeterminate */
    indeterminate?: boolean;
}
/** A Checkbox component. */
export declare class Checkbox extends ToggleableFoundationComponent<MDCCheckboxFoundation, CheckboxProps> {
    static displayName: string;
    private nativeCb;
    private root;
    constructor(props: CheckboxProps);
    sync(nextProps: CheckboxProps): void;
    getDefaultFoundation(): MDCCheckboxFoundation;
    handleAnimationEnd(evt: React.AnimationEvent): void;
    handleOnChange(evt: React.ChangeEvent<HTMLInputElement>): void;
    render(): {} | null | undefined;
}
